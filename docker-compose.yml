services:
  wxpush:
    build: .
    ports:
      - "${PORT:-40001}:${PORT:-40001}"
    environment:
      PORT: "${PORT:-40001}"
    volumes:
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
