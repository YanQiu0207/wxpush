services:
  wxpush:
    build: .
    ports:
      - "${PORT:-3939}:${PORT:-3939}"
    environment:
      PORT: "${PORT:-3939}"
    volumes:
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
